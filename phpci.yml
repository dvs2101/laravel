build_settings:
  verbose: false
  prefer_symlink: false

setup:
  composer:
    action: "install"
    prefer_dist: false

  php_unit:
    config:
      - "phpunit.xml"
    path: "tests/"

failure:
  email:
    committer: true
    default_mailto_address: dvs2101@gmail.com

complete:
  email:
    default_mailto_address: dvs2101@gmail.com
